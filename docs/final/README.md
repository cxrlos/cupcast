# docs/final

Final paper encompassing all Cupcast findings: the v1 Dixon-Coles baseline, the
v2 bounded-context model, and the held-out WC2026 tournament results.

Populate once the tournament is complete and the full scoring comparison (v1 vs
v2 vs market) can be included.

- `tex/` — LaTeX source for the final paper
- `pdf/` — compiled output (added when the paper is ready to build)
